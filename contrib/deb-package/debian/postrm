#!/bin/sh

#DEBHELPER#

if [ "$1" = "purge" ] ; then
  # remove user/group on purge
  deluser --quiet --remove-home oversip &>/dev/null || true
  delgroup --quiet  oversip &>/dev/null || true
  # remove /etc/oversip if empty
  rmdir /etc/oversip || true
fi

exit 0
